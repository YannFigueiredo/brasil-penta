export default function NotFound(){
    return(
        <div className="pagina-inexistente" style={{minHeight: '62vh', display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
            <h1 className="titulo">Página não encontrada!</h1>
        </div>
    );
}